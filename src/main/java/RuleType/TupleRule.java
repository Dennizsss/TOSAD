package RuleType;

public class TupleRule implements RuleTypeStrategy {

    /** Restriction on two or more attributes within a row
     * Shape: Entity - Column - Operator - customValue - operator - Column
     * Example: Employee where EmployeeName = "Sales" must have commision
     */
}
